import React from 'react';
import {Text, View} from 'react-native';
import Header from '../../component/Header';
import Strings from '../../locale';

const Profile = () => {
  return (
    <View>
      {/* <Header headerTitle={Strings.showroom} /> */}
      <Text> Profile </Text>
    </View>
  );
};

export default Profile;
